# Intelligent Housekeeper 后端依赖
# 安装: pip install -r requirements.txt

# Web框架
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
aiofiles==24.1.0

# 数据库
sqlalchemy==2.0.35
alembic==1.13.0
pydantic==2.9.0
pydantic-settings==2.5.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# AI / LLM
langchain==0.3.0
langchain-openai==0.2.0
langchain-core==0.3.0

# 爬虫
requests==2.32.0
beautifulsoup4==4.12.0
lxml==5.3.0
feedparser==6.0.11

# 文档处理
python-docx==1.1.2
openpyxl==3.1.5
pandas==2.2.0
PyMuPDF==1.24.0
python-pptx==1.0.2

# 本地嵌入兜底(sentence-transformers 依赖 torch/transformers)
transformers==4.45.0
torch==2.4.0
torchvision==0.19.0

# 定时任务
apscheduler==3.10.4

# MCP
mcp==1.0.0

# 飞书长连接(WebSocket 免公网)
lark-oapi

# 邮件
# smtplib (标准库)

# Redis
redis==5.1.0

# 向量库
chromadb==0.5.0
pymilvus==3.0.1
milvus-lite

# LLM HTTP 调用
httpx>=0.27.1

# SKILL 管理
PyYAML==6.0.3

# 记忆向量化（可选，有降级）
# sentence-transformers==3.0.0

# 快捷键
keyboard==0.13.5
